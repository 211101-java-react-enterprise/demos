export class Principal {

    id: string;
    username: string;
    role: string;

    constructor(id: string, un: string, role: string) {
        this.id = id;
        this.username = un;
        this.role = role;
    }
    
}